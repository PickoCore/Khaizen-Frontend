import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Minecraft Texture Pack Optimizer',
  description: 'Optimize your Minecraft texture packs for better performance and smaller file sizes',
  keywords: ['minecraft', 'texture pack', 'optimizer', 'compression', 'resource pack'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
